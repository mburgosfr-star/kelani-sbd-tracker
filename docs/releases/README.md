# Release notes archive

Public release notes are stored here by app version.

The guarded release workflow reads the file for the current version from:

```text
docs/releases/release-notes-vX.Y.Z.md
```

Create and validate release notes through the guarded release automation.
